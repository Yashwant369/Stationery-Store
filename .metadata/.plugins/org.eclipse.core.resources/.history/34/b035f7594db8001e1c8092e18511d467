package com.yashwant.stationerystore;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.security.crypto.password.PasswordEncoder;

@SpringBootApplication
public class StationeryStoreApplication {

	@Autowired
	public PasswordEncoder encoder;
	
	public String val = encoder.encode("yash123");
	
	
	public static void main(String[] args) {
		SpringApplication.run(StationeryStoreApplication.class, args);
		StationeryStoreApplication obj = new StationeryStoreApplication();
		
		
		System.out.println(obj.val);
		
		
		
	}
	
	
	

}
